var __addDisposableResource = (this && this.__addDisposableResource) || function (env, value, async) {
    if (value !== null && value !== void 0) {
        if (typeof value !== "object" && typeof value !== "function") throw new TypeError("Object expected.");
        var dispose, inner;
        if (async) {
            if (!Symbol.asyncDispose) throw new TypeError("Symbol.asyncDispose is not defined.");
            dispose = value[Symbol.asyncDispose];
        }
        if (dispose === void 0) {
            if (!Symbol.dispose) throw new TypeError("Symbol.dispose is not defined.");
            dispose = value[Symbol.dispose];
            if (async) inner = dispose;
        }
        if (typeof dispose !== "function") throw new TypeError("Object not disposable.");
        if (inner) dispose = function() { try { inner.call(this); } catch (e) { return Promise.reject(e); } };
        env.stack.push({ value: value, dispose: dispose, async: async });
    }
    else if (async) {
        env.stack.push({ async: true });
    }
    return value;
};
var __disposeResources = (this && this.__disposeResources) || (function (SuppressedError) {
    return function (env) {
        function fail(e) {
            env.error = env.hasError ? new SuppressedError(e, env.error, "An error was suppressed during disposal.") : e;
            env.hasError = true;
        }
        var r, s = 0;
        function next() {
            while (r = env.stack.pop()) {
                try {
                    if (!r.async && s === 1) return s = 0, env.stack.push(r), Promise.resolve().then(next);
                    if (r.dispose) {
                        var result = r.dispose.call(r.value);
                        if (r.async) return s |= 2, Promise.resolve(result).then(next, function(e) { fail(e); return next(); });
                    }
                    else s |= 1;
                }
                catch (e) {
                    fail(e);
                }
            }
            if (s === 1) return env.hasError ? Promise.reject(env.error) : Promise.resolve();
            if (env.hasError) throw env.error;
        }
        return next();
    };
})(typeof SuppressedError === "function" ? SuppressedError : function (error, suppressed, message) {
    var e = new Error(message);
    return e.name = "SuppressedError", e.error = error, e.suppressed = suppressed, e;
});
import { assert, test } from "@hazae41/phobos";
import { Buffer } from "node:buffer";
import { Memory, Secp256k1SigningKey, Secp256k1VerifyingKey, load } from "./mod.js";
function equals(a, b) {
    const ba = Buffer.from(a);
    const bb = Buffer.from(b);
    return ba.equals(bb);
}
test("sign", async () => {
    const env_1 = { stack: [], error: void 0, hasError: false };
    try {
        await load();
        const hash = __addDisposableResource(env_1, new Memory(crypto.getRandomValues(new Uint8Array(32))), false);
        const keypair = __addDisposableResource(env_1, Secp256k1SigningKey.from_bytes(new Memory(crypto.getRandomValues(new Uint8Array(32)))), false);
        const identity = __addDisposableResource(env_1, keypair.publish(), false);
        const signaturex = __addDisposableResource(env_1, keypair.sign_prehash_recoverable(hash), false);
        const signaturem = __addDisposableResource(env_1, signaturex.to_rsv_bytes(), false);
        const signatureb = signaturem.bytes;
        const r = signatureb.subarray(0, 32);
        const s = signatureb.subarray(32, 64);
        const v = signatureb[64];
        console.log(r, s, v);
        const identity2 = __addDisposableResource(env_1, Secp256k1VerifyingKey.recover_from_prehash(hash, signaturex), false);
        const midentity = __addDisposableResource(env_1, identity.to_sec1_compressed_bytes(), false);
        const midentity2 = __addDisposableResource(env_1, identity2.to_sec1_compressed_bytes(), false);
        assert(equals(midentity.bytes, midentity2.bytes));
    }
    catch (e_1) {
        env_1.error = e_1;
        env_1.hasError = true;
    }
    finally {
        __disposeResources(env_1);
    }
});
