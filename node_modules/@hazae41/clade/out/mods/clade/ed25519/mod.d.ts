export declare class Ed25519SeedKey {
    readonly seed: Uint8Array<ArrayBuffer>;
    constructor(seed: Uint8Array<ArrayBuffer>);
    /**
     * Generate the master private key using SLIP-0010 (~BIP-32)
     * @returns master private key
     */
    generate(): Promise<Ed25519ExtendedPrivateKey>;
    /**
     * Derive recursively to the child private key at the BIP-44 path
     * @param path
     * @returns child private key
     */
    derive(path: string): Promise<Ed25519ExtendedPrivateKey>;
}
export declare class Ed25519ExtendedPrivateKey {
    readonly key: Uint8Array<ArrayBuffer>;
    readonly ext: Uint8Array<ArrayBuffer>;
    constructor(key: Uint8Array<ArrayBuffer>, ext: Uint8Array<ArrayBuffer>);
    /**
     * Derive the child private key at index using SLIP-0010 (~BIP-32)
     * @param index
     * @returns child private key
     */
    derive(index: number): Promise<Ed25519ExtendedPrivateKey>;
}
